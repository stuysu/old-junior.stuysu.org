<table id="main">
    <tr>
        <td>

            <table class="content">

                <% let date = getDateFromSql(event.date); %>

                <tr>
                

                <td id="img">

                    <% if (event.url) { %>
                        <a target="_blank" id="#" href="<%= event.url %>">
                    <% } %>

                    <% if (event.poster) { %>
                        <img style="width: 90vw !important;" src="<%= event.poster %>" />
                    <% } else { %> 
                        <img style="width: 90vw !important;" src="/images/default-event-poster.png" />
                    <% } %>

                    <% if (event.url) { %>
                        </a>
                    <% } %>
                    
                </td>

            </tr>

                <tr>

                <td id="content-body">

                    <a id="a" style="font-size: 3.0vw;" href="/mobile/events/<%= event.id %>"><h1><%= event.title %></h1></a>
                    
                    <p style="font-size: 2.2vw;"><%= getDateLine(date) %> @ <%= getTimeLine(date) %></p>

                    <p>
                    <% if (event.url) { %>
                        <a style="color:inherit; font-size: 5vw;" target="_blank" id="#" href="<%= event.url %>">Click to see more</a>
                        |
                    <% } %>
                        <a style="color:inherit; font-size: 2.0vw;" href="/mobile/">Go back</a>
                    </p>

                </td>

                </tr>

                <tr class="spacer"></tr>

                <tr>
                    <td colspan="2" class="full-description">
                        <% if (event.description) { %>
                            <%- marked(event.description) %>
                        <% } %>
                    <td>
                </tr>

            </table>
        </td>
    </tr>
</table>